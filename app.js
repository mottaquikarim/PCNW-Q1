/*
  @func avgTwoNumbers
  @param a {number}
  @param b {number}
  @returns {number}
  @desc - computes average of two numbers
          formular for average: a + b / 2
  @example avgTwoNumbers(1,2); // 1.5
*/


const avarageTwoNumbers = (num1,num2) => {
    return (num1 + num2) / 2
}

console.log(avarageTwoNumbers(5,15))